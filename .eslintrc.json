{
  "extends": ["next/babel", "next/core-web-vitals"],
  "rules": {
    // Add or override ESLint rules here
    "react/react-in-jsx-scope": "off", // Disable the rule that enforces importing React when using JSX
    "no-console": ["error", { "allow": ["warn", "error"] }] // Customize the handling of console statements
  }
}